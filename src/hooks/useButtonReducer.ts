import routes from "../app.routes";
import { PageName } from "../app/(user)/feed/ButtonsList";

/* const initialState = {
    activePage: null,
    linkPage: null
};

function buttonReducer(state, action) {
    switch (action.type) {
        case 'FEED': return {
            ...state,
            activePage: PageName.Feed,
            linkPage: routes.Feed,
        };
        default:
            break;
    }

}

export function useButtonReducer() {
    return buttonReducer;
} */
